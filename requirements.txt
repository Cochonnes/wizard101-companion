# W101 Boss Wiki - Local Edition
# Install: pip install -r requirements.txt

# Core GUI
PyQt5>=5.15.0

# Database builder — nodriver opens real Chrome to bypass Cloudflare,
# then we use the cookies with requests to hit the MediaWiki API
nodriver>=0.38
requests>=2.28.0
beautifulsoup4>=4.12.0

# Fallback scraper (used by boss_wiki.py GUI "Fetch from Wiki" button)
cloudscraper>=1.2.60

# Optional: OCR auto-detect (full screen scanning)
# Uncomment to enable:
# easyocr>=1.7.0
# numpy>=1.24.0
# Pillow>=10.0.0

# Optional: OCR auto-detect (full screen scanning)
# Uncomment to enable:
# easyocr>=1.7.0
# numpy>=1.24.0
# Pillow>=10.0.0
